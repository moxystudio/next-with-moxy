{
    "version": 2,
    "name": "{project-name}",
    "public": true,
    "builds": [{ "src": "package.json", "use": "@now/next" }],
    "build": {
        "env": {
            "GA_TRACKING_ID": "@ga_tracking_id"
        }
    }
}
