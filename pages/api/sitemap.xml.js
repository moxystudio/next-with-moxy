import createSitemapApiHandler from '@moxy/next-sitemaps';

export default createSitemapApiHandler({
    mapDynamicRoutes: {
        // ... map the dynamic routes here. See step 2 of the docs, usage section: https://www.npmjs.com/package/@moxy/next-sitemaps#usage
    },
});
